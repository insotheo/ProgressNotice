<UserControl x:Class="ProgressNotice.CustomControls.TopMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProgressNotice.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="700">
    <Border CornerRadius="5" x:Name="TopArea" Background="Transparent">
        <Grid>
            <TextBlock Name="WindowTitleTB" VerticalAlignment="Center" Foreground="Black" FontSize="16" FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="_" Name="HideBtn"/>
                <Button Content="▭" Name="ResizeBtn"/>
                <Button Content="X" Name="CloseBtn"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
